# NX-Archivist 🏴‍☠️ — Pirate Cloud для Nintendo Switch

**NX-Archivist** — це автоматизована система для створення власного "хмарного" сховища ігор Nintendo Switch у Telegram.

---

## 📖 Повна інструкція з налаштування (від А до Я)

### Крок 1: Підготовка Telegram (Бот та Юзербот)

#### 1.1. Створення основного бота
1. Відкрийте [@BotFather](https://t.me/BotFather) у Telegram.
2. Напишіть `/newbot`.
3. Виберіть ім'я (наприклад, `My NX Archivist`) та юзернейм (має закінчуватися на `_bot`).
4. **Збережіть API Token** (виглядатиме як `123456:ABC-DEF...`).

#### 1.2. Отримання API ID та API Hash (для завантаження великих файлів)
1. Перейдіть на [my.telegram.org](https://my.telegram.org).
2. Увійдіть за своїм номером телефону.
3. Перейдіть у **API development tools**.
4. Створіть новий додаток (App title та Short name — будь-які).
5. **Збережіть `api_id` та `api_hash`**.

#### 1.3. Створення каналу-сховища
1. Створіть **приватний канал** у Telegram.
2. Додайте вашого бота (з кроку 1.1) як **адміністратора**.
3. Додайте себе (акаунт з кроку 1.2) як **адміністратора**.
4. Отримайте ID каналу: перешліть будь-яке повідомлення з каналу боту [@userinfobot](https://t.me/userinfobot). ID має починатися на `-100`.

---

### Крок 2: Налаштування RuTracker (Кукі)

Бот шукає ігри від вашого імені. Для цього йому потрібні кукі авторизації.

1. Зайдіть на [rutracker.org](https://rutracker.org) у браузері та увійдіть у свій акаунт.
2. Натисніть **F12** (Інструменти розробника).
3. Перейдіть на вкладку **Application** (у Chrome/Edge) або **Storage** (у Firefox).
4. У меню зліва розгорніть **Cookies** -> `https://rutracker.org`.
5. Створіть у папці з проектом файл `cookies.json` та вставте туди значення:
```json
{
  "bb_session": "значення_з_колонки_Value",
  "bb_t": "значення_з_колонки_Value",
  "bb_guid": "значення_з_колонки_Value",
  "cf_clearance": "значення_з_колонки_Value"
}
```

---

### Крок 3: Встановлення та запуск

#### 3.1. Налаштування файлу `.env`
1. Скопіюйте файл `.env.example` та назвіть його просто `.env`.
2. Відкрийте його блокнотом та впишіть ваші дані:
   - `BOT_TOKEN` (з кроку 1.1)
   - `API_ID` та `API_HASH` (з кроку 1.2)
   - `STORAGE_CHANNEL_ID` (з кроку 1.3)
   - `ENCRYPTION_PASSWORD` (придумайте пароль для ваших архівів)

#### 3.2. Запуск на Windows
1. Просто запустіть файл `run.bat`. 
2. Він автоматично:
   - Перевірить наявність `.env`.
   - Оновить код з GitHub.
   - Створить віртуальне оточення (`venv`).
   - Встановить усі необхідні бібліотеки.
   - Запустить бота.

#### 3.3. Запуск на Linux
1. Відкрийте термінал у папці з ботом.
2. Виконайте:
   ```bash
   chmod +x run.sh
   ./run.sh
   ```

---

## 🛠 Як це працює (Принцип системи)

1. **Пошук**: Ви пишете боту назву гри. Він шукає її на RuTracker.
2. **Дедуплікація**: Бот перевіряє базу даних. Якщо ви вже качали цей файл раніше (навіть в іншому релізі), він не буде качати його знову, а просто дасть посилання на старе повідомлення.
3. **Архівація**: Файли пакуються в `.7z` з паролем та випадковою назвою (для безпеки).
4. **Спліттінг**: Якщо файл більший за 2ГБ (або 4ГБ з Premium), він розбивається на частини.
5. **Доставка**: Ви отримуєте список посилань на повідомлення у вашому каналі.

---

## 🔍 Що робити, якщо не працює?

### Помилка "DLL load failed while importing libtorrent" (Windows)
Це означає, що у вашій системі відсутні компоненти Microsoft Visual C++.
**Рішення:**
1. Завантажте та встановіть **Visual C++ Redistributable 2015-2022**: [Завантажити (x64)](https://aka.ms/vs/17/release/vc_redist.x64.exe).
2. Після встановлення перезавантажте комп'ютер.

### Помилка "ImportError: No module named 'libtorrent'" (Linux)
На Linux бібліотеку `libtorrent` іноді краще встановлювати через системний пакетний менеджер.
**Рішення (для Ubuntu/Debian):**
```bash
sudo apt update
sudo apt install python3-libtorrent
```
Або спробуйте встановити через pip: `pip install libtorrent`.

### Інші проблеми
1. Перевірте файл **`bot.log`** у папці з проектом. Там записуються всі помилки.
2. Переконайтеся, що ви заповнили **всі** поля у `.env`.
3. Переконайтеся, що файл `cookies.json` має правильний формат JSON.
